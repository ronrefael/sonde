[sonde]
theme = "catppuccin-mocha"
lines = [
  "$sonde.model $sonde.session_clock $sonde.context_bar $sonde.usage_limits $sonde.pacing $sonde.agent $sonde.worktree $sonde.git_branch $sonde.active_sessions $sonde.mascot_icon",
  "$sonde.promo_badge",
]
